<Application x:Class="MLNetAnomalyDetection.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="clr-namespace:H.NotifyIcon;assembly=H.NotifyIcon.Wpf"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ContextMenu x:Key="TrayMenu">
            <MenuItem Header="Open Dashboard" Click="MenuOpen_Click" FontWeight="Bold" />
            <Separator />
            <MenuItem Header="About" Click="MenuAbout_Click" />
            <MenuItem Header="Exit" Click="MenuExit_Click" />
        </ContextMenu>

        <tb:TaskbarIcon x:Key="NotifyIcon"
                        ToolTipText="ML.NET Anomaly Detection: Starting..."
                        ContextMenu="{StaticResource TrayMenu}">
        </tb:TaskbarIcon>
    </Application.Resources>
</Application>
